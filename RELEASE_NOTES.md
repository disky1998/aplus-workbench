## v1.3.1

**修复 A+ 判定的两个真 bug**：品牌故事被当成高级 A+、轮播翻页内容没进 HTML。

### 修复

- 🔴 **品牌故事被误判成高级 A+，真实 A+ 反被丢掉**
  亚马逊的品牌故事块内部套了一个 `<div id="aplus">`，而真正的 A+ 放在 `#aplus_feature_div` 里。
  旧逻辑按 id 取 `#aplus` 正好取到品牌故事（里面有 1464px 的 hero 图）→ 判成高级 A+；又因为
  「`#aplus` 有内容就丢掉 `#aplus_feature_div`」，把真实 A+ 一起扔了。
  实测 `B08TSSDWFL`：品牌故事图泄漏 **5/5 → 0/5**，容器 `aplus` → `aplus_feature_div`，
  类型 **高级 A+ → 普通 A+**，并正确跳过移动端。
  修复：排除时**沿祖先链检查**、同名 id **全部遍历**、不再按名字丢容器、模块级再过滤一次品牌故事。

- 🔴 **普通 A+ 被误判成高级 A+**
  判定阈值原为「图宽 > 970」，但普通 A+ 的 `module-9`/`module-2` 模块图宽实测就是 **1000px**。
  现在改为**类名优先**（`premium`/`aplus-premium` vs `aplus-standard`/`3p-module`），图宽阈值提到 **1400**。

- 🔴 **移动端轮播（左右翻页）的图和版式没被记录**
  原页面用 JS 驱动横向轨道（`.a-carousel-viewport{overflow:hidden}` + 轨道固定 3510px），
  克隆到离线 HTML 后第二屏之后全被裁掉。
  现在：解析层按 `-slide-N` 序号**逐屏提取**（DOM 顺序是乱的），渲染层把轮播改成**横向可滚动的静态排布**，
  所有屏的图和版式都保留、可左右滚动翻看。
  实测 `B0F4X77JZW`：4 个轮播 **2+3+3+2 = 10 屏全部识别并写入 HTML**，桌面端品牌故事也清零。

- 🟡 **偶尔整页抓不到 A+**：同一链接可能返回不含 A+ 容器的简化版页面（实测带 `?th=1` 2.8MB 完整页、
  不带仅 531KB 且容器都不存在）。现在**容器都没有时自动重载再解析**，不再直接判定「无 A+」。

### 变更

- 剔除了 `aplus-mantle` 这类没有图也没有文案的空壳模块，「模块数」不再虚高
- A+ 判定依据（`premium_evidence`）去重，只保留最关键的一条

---

## v1.3.0

**预热浏览器 + 多路并行抓取**，导出收敛成两张表，结果页精简分页。

### 新增

- 🔥 **浏览器预热**：一次启动 6 个标签页（有头浏览器），6 个标签页**共享同一份登录态** —— 在任意一个里登录 / 过验证码，其余全部生效。预热过程每一步都有进度提示
- ⚡ **多路并行抓取**：预热完成后，6 个线程各用一个标签页同时开抓，实测 3 路并行 34 秒跑完 3 个 SKU（单个商品本身要 18-19 秒）
- **开始按钮受预热门控**：没预热时置灰并给出提示；也可勾选「跳过预热」用原来的单线程方式抓

### 变更

- **导出只保留两张表**（CSV 与 Excel 列完全相同，已合并）：
  - `商品信息表` —— 标题 / 亮点 / 类目 / NodeID / 五点 / 主图 / 品牌 / 价格 / 评分 / 评论 / 卖家
  - `批量改写导入表` —— 前 8 列严格对齐「批量文案重构」的导入要求（SKU / 原标题 / 原亮点 / 五点1..五点5），后面是溯源列，可直接上传
- **结果页精简**：只留 `SKU/ASIN`、`标题`、`关键数据`、`状态` 四列，超过 20 条自动分页，不再一屏铺开几十列
- **移除「重建合集 / 重新打包图片」**：合集与图片包仍在每次抓取结束时自动生成，历史产出只需要下载
- 退出程序时会一并关闭预热浏览器

### 修复

- 🔴 全新环境下第一次预热会**卡在「连接调试端口」**：全新 Chrome profile 首次启动时浏览器级 CDP WebSocket 能连上但握手会卡死（`Timeout 20000ms exceeded`）。现在预热前会先用同一 profile 做一次静默初始化，实测全新环境 25 秒就绪，不再卡死

### 运行要求

- Windows 10 / 11 64 位
- 首次使用请先在「环境」面板点一次「安装 / 更新内核」（约 150 MB，只需一次）

---

## v1.2.1

**修复：exe 双击后报「服务启动失败：Unable to configure formatter 'default'」** —— 已彻底解决，直接换用这个版本即可。

### 修复

- 🔴 **exe 启动即失败**。无控制台打包（`console=False`）后 `sys.stdout` / `sys.stderr` 是 `None`，而 uvicorn 的日志格式化器初始化时会执行 `sys.stdout.isatty()`，抛出的 `AttributeError` 被 `logging.config` 包成了 `Unable to configure formatter 'default'`，服务根本起不来。源码运行（有控制台）不会触发，所以之前测试没暴露。
  - 现在启动时先把 `sys.stdout` / `sys.stderr` 补成真实可写的文件流
  - 同时换成不读 `isatty()` 的标准库日志配置，双保险
- 报错弹窗与异常兜底现在会显示**出错位置摘要**并给出日志文件路径，不再只有一句干巴巴的消息

### 新增

- **运行日志**：`logs/workbench.log`（程序日志）与 `logs/server.log`（服务输出）落在 exe 同目录；该目录不可写时自动退到 `%TEMP%\AmazonWorkbench\`。以后出问题直接把这两个文件发我就能定位

### 运行要求

- Windows 10 / 11 64 位
- 首次使用请在「环境」面板点一次「安装 / 更新内核」（约 150 MB，只需一次）

---

## v1.2.0

**合并为一个程序**：链接信息抓取（原来的采集器）+ A+ 内容抓取，统一入口、统一 exe。

### 新增

- **商品链接信息抓取**：标题、7.27 商品亮点（小标题）、类目路径、最小类目节点 ID、五点描述、主图（还原原始 master 大图）、品牌、价格、评分、评论数、卖家
- **「抓取内容」三选一**：`全部`（商品信息 + A+，共用同一个页面一次抓完）/ `商品信息`（不解析 A+，快很多）/ `A+ 内容`
- **商品信息表导出**：Excel（多列 + 图片 URL 列）与 CSV，一次任务的所有 SKU 汇总成一张表；历史产出也能随时导出
- 结果表新增「商品信息」列，直接看到标题、亮点、五点条数、主图张数、品牌、价格、评分、类目

### 变更

- 程序更名：**亚马逊采集工作台**（`AmazonWorkbench.exe`）
- **下载图片**、**有头模式** 改为默认开启（有头模式第一次跑一次过掉验证码，之后会话会被记住）
- 选中「商品信息」时，A+ 相关设置自动收起

### 保留

- A+ 抓取：普通 / 高级 A+（Premium A+），桌面端 + 移动端，三种模式（自动判定 / 强制普通 / 强制高级）
- 排版还原单文件 HTML、按 SKU 分节的 `combined.html`、A+ 图片包 `aplus_images.zip`（含 `manifest.csv`）
- 历史产出可一键重建合集 / 重新打包图片，不必重抓
- 桌面托盘、启动自动检查更新、一键下载并自动替换重启
- 浅色 / 深色双主题

### 运行要求

- Windows 10 / 11 64 位
- 首次使用请在「环境」面板点一次「安装 / 更新内核」（约 150 MB，只需一次）
